# OPTIONAL / PREBUILT ONLY

Birch override (Node notified): the **on-stage live-build** fixture is

**`checkout-error-log-analyzer`**

against `demo/acme-store/logs/checkout-errors.ndjson`
(naive 4 / correct 9 nested `context.customer.id`).

This `api-change-impact-analyzer` pack remains as an **optional / prebuilt**
capability fixture for non-demo paths. Do not wire it as the attempt-1 fail beat.
